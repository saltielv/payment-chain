# Server Port
server:
  port: 8083

# DataBase Configuration
spring:
  datasource:
    url: jdbc:h2:mem:transaction
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
  h2:
    console:
    enabled: true